import numpy as np
from pypower.api import case33bw, case30, runpf, ppoption
import matplotlib.pyplot as plt
import random
import pandapower.networks as pn

# Initialize
ppc = case33bw()
# ppc = case30()
# ppc['branch'][:,2] = ppc['branch'][:,2]/(12.66**2/10)
# ppc['branch'][:,3] = ppc['branch'][:,3]/(12.66**2/10)
# ppc['bus'][:,2] = ppc['bus'][:,2]/1000
# ppc['bus'][:,3] = ppc['bus'][:,3]/1000

ppopt = ppoption(PF_ALG=1, VERBOSE=1, OUT_ALL=1)
result, success = runpf(ppc)

